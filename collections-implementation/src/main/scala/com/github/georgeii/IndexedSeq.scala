package com.github.georgeii

import com.github.georgeii

/**
 * The key feature of IndexedSeq is a constant-time (or close to constant-time) complexity of accessing any element.
 * @tparam A type of its elements.
 */
trait IndexedSeq[A] extends georgeii.Seq[A]{

}
